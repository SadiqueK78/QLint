# Python
.venv/
backend/__pycache__/
backend/**/__pycache__/
*.pyc
.pytest_cache/

# Secrets — never commit
backend/.env

# Frontend
frontend/node_modules/
frontend/dist/
# Per-machine Vite overrides. frontend/.env itself IS committed -- it is the
# documented default and holds no secrets.
frontend/.env.local
frontend/.env.*.local

# Docker — per-machine compose tweaks, from the .example file
docker-compose.override.yml

# Editor/OS noise
.vscode/
.idea/
.DS_Store